{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        // outDir は dist/server に
        "outDir": "./dist/",
        "module": "CommonJS",
        "target": "ES2020"
        // "rootDir": "./src"  ← 継承先 (tsconfig.json) をそのまま使う
    },
    "include": [
        // サーバー側のソースを含む
        "src/server/**/*",
        // もし共通の型定義があるなら、types もコンパイル対象に含む
        "src/types/**/*"
    ],
    "exclude": [
        // クライアント側は含まない
        "src/client/**/*",
        "node_modules"
    ]
}